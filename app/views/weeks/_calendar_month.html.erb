
<% content_for :top do %>

		<div class="title"><%= @start_date.strftime('%B') %> </div>
		<%= link_to '<', week_month_path(@start_date - 5.days), :remote => true %>
		<%= link_to 'This Month', week_month_path(Date.today()), :remote => true %>
		<%= link_to '>', week_month_path(@end_date + 5.days), :remote => true %>
		<div class="ribbon"></div>

<% end %>

<% content_for :page do %>
	
	<%#= raw(event_calendar) %>
	
<% end %>

<%= render '/home/pad' %>
