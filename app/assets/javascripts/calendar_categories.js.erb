$('#calendar_categories .category .top').live('click',function(){
	var category = $(this).parent();
	$('#calendar_categories .active ul').slideUp(300, function(){
		$('#calendar_categories .category').removeClass('active');
		category.addClass('active');
		category.find('ul').first().slideDown(300);
	});
	
});